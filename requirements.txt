streamlit==1.30.0  # Upgraded from 1.21.0 for compatibility with pandas 2.1.0
numpy>=1.26       # Changed from 1.24.3 for compatibility with Python 3.12
pandas==2.1.0
matplotlib==3.7.1
scikit-learn==1.2.2
lightgbm==3.3.5
xgboost==1.7.5
imblearn==0.0   # Please double-check if "imblearn" is the correct package name and "0.0" is the intended version. If you meant "imbalanced-learn", update this line.
# sqlite3 is a built-in Python module and does not need to be listed here.
